<% provide(:title, @user.name) %>
<%= form_with(model: @user, url: attendances_update_one_month_user_path(date: @first_day), local: true, method: :patch) do |f| %>
<div>
  <h1>勤怠編集画面</h1>
  <table class="table table-bordered table-condensed table-hover" id="table-attendances">
    <thead>
      <tr>
        <th>日付</th>
        <th>曜日</th>
        <th>出勤時間</th>
        <th>退勤時間</th>
        <th>翌日</th>
        <th>在社時間</th>
        <th>備考</th>
        <th>指示者確認㊞</th>
      </tr>
    </thead>
    <tbody>
        <% @attendances.each do |day| %>
         <%= f.fields_for "attendances[]", day do |attendance| %>
          <tr>
           <td><%= l(day.worked_on, format: :short) %></td>
           <td>
           <% if day.worked_on.wday == 6 %>
            <font color="Blue">土</font>
           <% elsif day.worked_on.wday == 0 %>
            <font color="RED">日</font>
           <% else %>
            <%= $days_of_the_week[day.worked_on.wday] %>
           <% end %>
           </td>
           <% if !current_user.admin? && (Date.current < day.worked_on) %>
            <td><%= attendance.time_field :started_at, readonly: true, class: "form-control" %></td>
            <td><%= attendance.time_field :finished_at, readonly: true, class: "form-control" %></td>
           <% else %>
            <td><%= attendance.time_field :started_at, class: "form-control" %></td>
                <%= attendance.hidden_field :second_start_time, value:  day.started_at%>
            <td><%= attendance.time_field :finished_at, class: "form-control" %></td>
                <%= attendance.hidden_field :second_end_time, value:  day.finished_at%>
           <% end %>
           <td><%= attendance.check_box :next_day_flag, class: "form-control" %></td>
           <td>
            <% if day.started_at.present? && day.finished_at.present? %>
             <% if day.next_day_flag? %>
              <% wk_time = 24 %>
              <% working_times(day.started_at, day.finished_at) %>
              <%= working_times(day.started_at, day.finished_at).to_f + wk_time.to_f %>
             <% else %>
              <%= working_times(day.started_at, day.finished_at) %>
             <% end %>
            <% end %>
           </td>
           <td><%= attendance.text_field :note, class: "form-control" %></td>
           <td><%= attendance.select :change_superior_id, User.where(superior: true).pluck(:name),include_blank: true,class: "form-control" %></td>
               <%= attendance.hidden_field :change_status, value: 3%>
          </tr>
         <% end %>
        <% end %>
    </tbody>
  </table>
</div>
<div class="center">
  <%= f.submit "まとめて更新", class: "btn btn-lg btn-primary" %>
  <%= link_to "キャンセル", user_path(date: @first_day), class: "btn btn-lg btn-default" %>
</div>
<% end %>